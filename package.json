{
  "name": "typescripttesting",
  "version": "1.0.0",
  "description": "",
  "main": "try.js",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && node ./build/classChapter.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "reflect-metadata": "^0.1.13"
  }
}
